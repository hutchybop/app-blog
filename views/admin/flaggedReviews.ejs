<% layout('layouts/boilerplate') %>

<div class="container mt-4">
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-header d-flex justify-content-between align-items-center">
                    <h3>Flagged Reviews for Admin Review</h3>
                    <div>
                        <a href="/admin" class="btn btn-outline-primary">Admin Dashboard</a>
                        <a href="/blogim" class="btn btn-secondary">Back to Blog</a>
                    </div>
                </div>
                <div class="card-body">
                    <!-- Review Statistics -->
                    <div class="row mb-4">
                        <div class="col-md-3">
                            <div class="card bg-info text-white">
                                <div class="card-body text-center">
                                    <h5>Total Flagged</h5>
                                    <h2><%= flaggedReviews.length %></h2>
                                </div>
                            </div>
                        </div>
                        <div class="col-md-3">
                            <div class="card bg-warning text-dark">
                                <div class="card-body text-center">
                                    <h5>High Spam (≥10)</h5>
                                    <h2><%= flaggedReviews.filter(r => r.spamScore >= 10).length %></h2>
                                </div>
                            </div>
                        </div>
                        <div class="col-md-3">
                            <div class="card bg-secondary text-white">
                                <div class="card-body text-center">
                                    <h5>Moderate Spam (3-9)</h5>
                                    <h2><%= flaggedReviews.filter(r => r.spamScore >= 3 && r.spamScore < 10).length %></h2>
                                </div>
                            </div>
                        </div>
                        <div class="col-md-3">
                            <div class="card bg-success text-white">
                                <div class="card-body text-center">
                                    <h5>Low Spam (1-2)</h5>
                                    <h2><%= flaggedReviews.filter(r => r.spamScore >= 1 && r.spamScore < 3).length %></h2>
                                </div>
                            </div>
                        </div>
                    </div>

                    <% if (flaggedReviews.length === 0) { %>
                        <div class="alert alert-info">
                            <h4>No flagged reviews to review</h4>
                            <p>All reviews are currently clean!</p>
                        </div>
                    <% } else { %>
                        <% flaggedReviews.forEach(review => { %>
                            <div class="card mb-3 border-warning">
                                <div class="card-header bg-warning text-dark">
                                    <div class="d-flex justify-content-between align-items-center">
                                        <div>
                                            <strong>Spam Score:</strong> <%= review.spamScore %> | 
                                            <strong>Flagged:</strong> <%= review.createdAt.toLocaleDateString() %>
                                        </div>
                                        <div>
                                            <span class="badge bg-danger">Requires Review</span>
                                        </div>
                                    </div>
                                </div>
                                <div class="card-body">
                                    <div class="row">
                                        <div class="col-md-8">
                                            <h6>Review Content:</h6>
                                            <p class="border p-3 bg-light"><%= review.body %></p>
                                            
                                            <% if (review.flagReason) { %>
                                                <h6>Flag Reason:</h6>
                                                <p class="text-warning"><%= review.flagReason %></p>
                                            <% } %>
                                        </div>
                                        <div class="col-md-4">
                                            <h6>Metadata:</h6>
                                            <ul class="list-unstyled">
                                                <li><strong>Author:</strong> <%= review.author ? review.author.username : 'Anonymous' %></li>
                                                <li><strong>IP Address:</strong> <%= review.ipAddress || 'N/A' %></li>
                                                <li><strong>Created:</strong> <%= review.createdAt.toLocaleString() %></li>
                                            </ul>
                                            
                                            <div class="mt-3">
                                                <form action="/admin/flagged-reviews/<%= review._id %>/approve" method="POST" class="d-inline">
                                                    <button type="submit" class="btn btn-success btn-sm">Approve</button>
                                                </form>
                                                <form action="/admin/flagged-reviews/<%= review._id %>/delete" method="POST" class="d-inline">
                                                    <button type="submit" class="btn btn-danger btn-sm" onclick="return confirm('Are you sure you want to delete this review?')">Delete</button>
                                                </form>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        <% }); %>
                    <% } %>
                </div>
            </div>
        </div>
    </div>
</div>